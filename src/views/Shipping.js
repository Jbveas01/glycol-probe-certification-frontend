const Shipping = () => {
  return <div>Shipping
    This page will add the ability to PUT a store location on each probe
    Displayed similarly to the probes table but with
    Serial ShipDate Division Store Tracking#

  </div>;
};

export default Shipping;
